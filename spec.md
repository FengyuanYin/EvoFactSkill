# EvoFactSkill Spec

> 修订说明：本文档在原 EvoFactSkill 规格基础上加入论文提出的 DEMSE（Domain-Episodic Meta-Gated Skill Evolution，域情景元门控技能进化）。DEMSE 是本项目跨域自进化的核心机制；原固定验证门控作为兼容模式和实验基线保留。

## 背景

现有 CD-FND 已验证固定多智能体链路、Skill 包、版本化与候选晋升机制，但当前进化主要是单个 Skill 的文本修改，验证样本少、指标可能奖励弃权，尚不具备 Skill 自动发现、生命周期管理、错误归因和负迁移控制。

EvoFactSkill 是完全独立的新项目，面向跨域与时间漂移场景，实现可验证的假新闻检测 SkillBank 自进化系统。底层大语言模型在实验期间保持冻结，系统通过进化外部 SkillBank、路由策略和工作流提升能力。

## 目标

- 从成功与失败轨迹中归纳可复用的事实核验 Skill。
- 支持 Skill 的新增、编辑、拆分、合并、泛化、特化和淘汰。
- 联合优化路由策略与事实核验 Skill。
- 使用错误归因确定应该修改的组件。
- 通过多域、多时间窗口验证和负迁移约束控制晋升。
- 同时优化检测性能、覆盖率、校准、成本和最差域表现。
- 接入 Weibo21、AMTCele、LiveFact 和 AdvFake。
- 提供完整的基线、消融、统计检验、版本审计和回滚能力。
- 保持底层模型冻结，将性能变化归因于 SkillBank 和工作流进化。
- 以完整领域为单位构造 meta-train/meta-test episode，在离散、可解释的 SkillBank 空间实现双层元学习。
- 由 meta-train 域生成候选 Skill，由 meta-test 域度量未知域迁移效用并控制外层更新。
- 跨多个领域 episode 聚合统计证据，区分可泛化、应特化、Pareto 保留和应淘汰的 Skill。

## 功能需求

- F1 数据与隔离：统一加载 Weibo21、AMTCele、LiveFact、AdvFake；支持按领域、事件和时间切分，保证训练、进化验证、最终测试互不泄漏。
- F2 推理链路：实现 Coordinator → Specialist → Evidence Aggregator → Judge，并保存完整结构化 trace。
- F3 动态 SkillBank：Skill 包含指令、资源、脚本、适用范围、触发条件、版本、成本与历史效用。
- F4 Skill 生命周期：支持 `ADD / EDIT / SPLIT / MERGE / GENERALIZE / SPECIALIZE / RETIRE / ROLLBACK`。
- F5 Skill 路由：根据样本内容、领域、时间和历史效用检索并选择少量 Skill；路由失败时采用确定性安全兜底。
- F6 错误归因：将失败归类为路由遗漏、证据遗漏、证据幻觉、时间泄漏、推理错误、聚合错误、标签错误和不当弃权。
- F7 经验蒸馏：从成功轨迹与错误簇中生成通用、领域、事件或时间敏感 Skill 候选，避免直接存储冗长原始轨迹。
- F8 联合进化：允许分别或联合产生路由 Skill、专家 Skill、Judge Skill、工具脚本和工作流候选。
- F9 候选验证：在固定分层验证集上采用配对重复评估、最小提升阈值和统计检验；最终测试集永不参与进化。
- F10 多目标门控：综合全样本 Macro-F1、覆盖率、选择性风险、校准、调用成本、延迟和最差域退化决定晋升。
- F11 负迁移控制：记录 Skill 的下游边际效用；对持续无效或损害其他域的 Skill 降权、停用或淘汰。
- F12 Pareto 版本库：保留性能、覆盖率、成本之间非支配候选，记录父版本、变更原因、验证结果和完整差异。
- F13 安全控制：新脚本必须通过路径、语法、导入、接口和危险操作检查；默认不允许候选直接访问标签或最终测试数据。
- F14 基线与消融：支持单 LLM、静态多智能体、全专家、随机路由、仅 Prompt 进化、无 Skill 发现、无负迁移控制、完整系统等实验臂。
- F15 统计报告：输出逐域/逐时间指标、置信区间、配对显著性检验、校准、覆盖率、成本和 Skill 演化曲线。
- F16 可复现运行：提供统一 CLI、配置快照、随机种子、断点续跑、测试替身和离线 dry-run。
- F17 人工治理：允许审核高风险候选、冻结 Skill、拒绝晋升，以及恢复任意历史版本。
- F18 Meta-Skill 慢循环：在积累足够进化记录后，允许优化错误分析、候选生成、资源分配和晋升策略；默认关闭并与普通 Skill 快循环隔离。
- F19 域注册：从 `Sample.domain` 和数据集来源建立领域清单；领域缺失、样本过少或数量不足时给出明确错误。
- F20 外层隔离：先划分源领域池与最终测试域；最终测试域不得进入 episode、候选生成、超参数选择或门控。
- F21 域级 Episode：支持重复随机域划分和留一域验证；单个 episode 的 meta-train/meta-test 领域必须完整、互斥，并保证全部源领域获得 meta-test 覆盖。
- F22 内层候选生成：仅使用 meta-train 域的脱敏推理轨迹、错误归因和蒸馏结果执行生命周期候选生成。
- F23 信息防火墙：候选生成器不得访问 meta-test 文本、标签、错误归因和指标；meta-test 信息仅允许进入只读评估与效用统计阶段。
- F24 Meta-test 配对评估：在相同样本、模型、预算和随机条件下比较当前与候选 SkillBank，输出 Macro-F1、coverage、ECE、selective risk、成本和逐域变化。
- F25 跨轮迁移效用：按候选语义身份聚合平均收益、标准差、置信区间下界、成功率、负迁移率和最差域退化。
- F26 外层元门控：综合统计显著性、覆盖率、成本、安全和最差域约束，将候选决策为 `active/generalized`、`specialized`、`pareto`、`retired/rejected` 或 `review_required`。
- F27 语义归并与原子更新：内容等价或同源候选跨 episode 稳定归并；meta-test 期间仓库只读，全部 episode 聚合完成后才允许一次原子更新。
- F28 断点续跑：持久化 episode 配置、领域划分、候选、配对结果和聚合状态；恢复时不得重复提交已完成 episode。
- F29 DEMSE CLI 与报告：增加 `meta-evolve` 命令、独立配置、领域轮换表、逐 episode 配对指标、候选迁移效用和状态变化报告。
- F30 DEMSE 对照与消融：支持固定验证门控、完整 DEMSE、无跨轮聚合、无最差域约束和无特化分支等实验臂。

## 非功能需求

- N1 独立性：所有代码、数据配置和输出仅位于 `F:\intern\Agent\auto-skill\EvoFactSkill`；不得修改 CD-FND。
- N2 可复现性：相同代码、配置、Skill 版本、数据切分和随机种子应产生可审计的等价实验过程；模型非确定性必须通过重复评估体现。
- N3 可测试性：核心路由、指标、错误归因、生命周期、门控和版本管理必须可脱离真实模型 API 进行单元测试。
- N4 可恢复性：训练或进化中断后能够从 checkpoint 继续；任何候选失败不得破坏当前 active SkillBank。
- N5 数据安全：Gold label 只能在预测完成后的评估和进化分析阶段出现；最终测试标签不得进入候选生成或晋升。
- N6 Skill 安全：候选脚本默认视为不可信内容，晋升前必须完成静态检查和受限执行验证，并保留人工审核入口。
- N7 可观测性：每次路由、模型调用、Skill 使用、错误归因、候选操作、晋升与回滚都应写入结构化日志。
- N8 成本可控：支持限制每样本 Agent 数、token、模型调用、验证样本量和并发数；报告实际调用成本。
- N9 向后兼容：能够导入现有 CD-FND 的基础 Skill 格式，但运行时不依赖或写入原项目。
- N10 模块化：数据、推理、SkillBank、进化、验证和评估之间使用稳定接口，能够替换模型提供方或新增数据集。
- N11 研究诚实性：不得把弃权样本从主性能指标分母中静默删除；不得用最终测试集选版本；异常运行不得包装成有效结果。
- N12 文档性：提供架构说明、数据协议、实验命令、指标定义、Skill 生命周期说明和复现实验指南。
- N13 运行环境：以 Python 为主要实现语言，兼容 Windows PowerShell；模型凭据只能通过环境变量或显式安全配置读取。
- N14 性能边界：离线单元测试不依赖网络；小样本 dry-run 应能在普通开发机上完成；完整模型实验允许长时间运行。
- N15 质量门槛：代码必须通过测试套件和基本静态检查，不提交缓存、密钥、运行输出或数据派生临时文件。
- N16 元学习边界：DEMSE 不更新底层模型参数，元更新对象仅限 Skill、路由规则及生命周期状态。
- N17 Episode 可复现性：相同数据指纹、配置、SkillBank 快照和随机种子应产生一致的 episode ID、顺序和领域划分。
- N18 元测试隔离：meta-test 数据只能进入评估器与聚合器，任何候选生成上下文均不得包含 meta-test 内容或标签。
- N19 元门控稳健性：不得以单个 episode 的偶然提升作为全局晋升依据；候选必须达到最小有效 episode 数。
- N20 向后兼容：旧配置未声明 meta-learning 字段时保持原固定门控行为，已有 CLI 和数据适配器继续可用。

## 不做的事

- 不训练、微调或强化学习更新底层大语言模型参数。
- 不实现真实社交平台的在线爬取、发布、举报或自动干预。
- 不把系统包装成面向公众的生产级事实核查服务或 Web 前端。
- 不声称模型生成的解释天然忠实；解释只作为可审计 trace，并通过证据一致性指标评估。
- 不允许系统使用测试集标签、文件名暗示、数据集前缀等泄漏信号生成或选择 Skill。
- 不自动执行未经检查的网络、文件写入、进程启动或任意代码型 Skill。
- 不将一次小样本提升直接视为有效进化；候选必须通过正式门控。
- 不在本轮实现图片、视频和音频的端到端多模态模型推理；仅预留多模态 Skill 接口。
- 不复刻 SkillRL 的模型参数级强化学习，也不把普通 Prompt 改写描述成完整 SkillRL。
- 不默认开启 Meta-Skill 自递归；先积累普通 Skill 进化记录，再通过配置显式启用。
- 不实现参数级 MAML、Reptile 或其他需要底层模型梯度更新的方法。
- 不把 meta-test 域作为传统训练数据，也不根据其具体错误内容直接改写 Skill。
- 不以样本级随机划分替代域级 episode，不将一次随机域划分的结果包装成稳定跨域泛化能力。
- 不删除现有固定验证门控；它继续作为兼容模式和论文对照基线。
- 不修改、移动、清理或提交 `F:\intern\Agent\auto-skill\CD-FND` 中的任何文件。
- 不在没有相应数据授权和许可确认时重新分发第三方数据集。

## 验收标准

- AC1：目标目录形成独立项目，运行和测试不会修改 CD-FND；通过前后 Git 状态对比验证。
- AC2：四个数据集均有统一加载入口；缺少或未授权的数据文件能给出明确提示，不影响其他数据集运行。
- AC3：训练、进化验证、最终测试按域、事件或时间严格隔离，并有自动泄漏检查。
- AC4：任一推理样本均产生包含路由、Skill 版本、专家报告、聚合、Judge 决策、成本和错误信息的 trace。
- AC5：SkillBank 能执行并持久化八种生命周期操作，非法操作不会改变 active 状态。
- AC6：系统能从一组失败轨迹形成错误簇，并输出结构化错误归因与对应候选 Skill 操作。
- AC7：成功轨迹能够被蒸馏为紧凑 Skill，且候选中不包含样本标签、答案或整段训练轨迹。
- AC8：路由器能够根据适用范围和历史效用选择 Skill，并对未知、空路由和已退休 Skill 安全处理。
- AC9：候选必须经过固定分层验证、重复配对评估、最小提升阈值和回归约束；单次随机提升不能直接晋升。
- AC10：主报告将弃权计入全样本指标，同时输出 coverage、selective risk、校准、成本和最差域性能。
- AC11：当候选提升平均性能但显著损害任一保护域时，门控拒绝候选或将其保留为非 active Pareto 候选。
- AC12：Skill 的使用次数、成功率、边际效用和负迁移记录可查询；持续有害 Skill 可被降权或淘汰。
- AC13：版本库支持查看父子关系、完整 diff、验证记录，并能恢复任意历史 active 版本。
- AC14：新增脚本型 Skill 未通过安全检查时无法进入端到端验证和 active SkillBank。
- AC15：统一 CLI 能运行 dry-run、训练/进化、验证、测试、消融、版本查看和回滚。
- AC16：至少提供八个实验臂，并在同一分层样本和统一指标口径下产生可比较报告。
- AC17：统计报告包含多 seed 汇总、置信区间、配对检验、逐域/逐时间结果和 Skill 演化曲线数据。
- AC18：Meta-Skill 默认关闭；启用后使用独立慢循环、独立版本记录和独立晋升门控。
- AC19：无网络、无 API Key 时全部单元测试和离线端到端 dry-run 可通过。
- AC20：README 提供安装、配置、数据准备、快速验证、正式实验和结果解释指南。
- AC21：完整测试套件与静态检查通过，且不会生成或提交密钥、缓存和实验输出。
- AC22：新增域级 episode 采样器；任一 episode 的 meta-train/meta-test 域无交集，相同输入与种子产生相同 episode。
- AC23：重复 episode 后每个源领域至少担任一次 meta-test 域；最终测试域不会出现在任何进化 episode 中。
- AC24：候选仅由 meta-train 脱敏信息产生；自动测试能够阻断 meta-test 内容、标签和最终测试信息泄漏。
- AC25：每个候选在相同 meta-test 样本上完成 baseline/candidate 配对评估，并报告主指标、覆盖率、校准、风险、成本和逐域变化。
- AC26：跨轮聚合正确计算平均迁移增益、标准差、置信区间下界、成功率、负迁移率和最差域退化；有效 episode 数不足时不得激活。
- AC27：稳定跨域增益候选可泛化晋升；局部稳定增益候选可特化；非支配候选可进入 Pareto；持续无效或高负迁移候选被拒绝或淘汰。
- AC28：meta-test 评估期间 SkillRepository 保持只读，全部 episode 完成后才发生原子版本更新，且历史可追溯来源 episode 和门控理由。
- AC29：checkpoint 恢复不会重复执行已完成 episode，恢复结果与连续执行等价。
- AC30：`evofact meta-evolve` 可在不少于五个领域的离线夹具上运行，并生成结构化 JSON 与 Markdown 报告。
- AC31：新增测试覆盖泛化、特化、Pareto、负迁移、泄漏阻断和最终测试只读，且全部原有测试保持通过。
- AC32：代码与报告明确将 DEMSE 定义为离散 SkillBank 空间元学习，不声称参数级梯度更新。
